import { ExactPackage, LatestPackageTarget, PackageConfig } from "../install/package.js";
import { Resolver } from "../trace/resolver.js";
export declare function pkgToUrl(pkg: ExactPackage): Promise<`${string}/`>;
export declare function parseUrlPkg(url: string): {
    registry: string;
    name: string;
    version: string;
};
export declare function getPackageConfig(this: Resolver, pkgUrl: string): Promise<PackageConfig | null>;
export declare function resolveLatestTarget(this: Resolver, target: LatestPackageTarget, layer: string, parentUrl: string): Promise<ExactPackage | null>;
