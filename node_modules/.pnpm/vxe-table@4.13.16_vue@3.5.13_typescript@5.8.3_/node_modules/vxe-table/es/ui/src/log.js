import { VxeUI } from '@vxe-ui/core';
const { log } = VxeUI;
const version = `table v${"4.13.16"}`;
export const warnLog = log.create('warn', version);
export const errLog = log.create('error', version);
